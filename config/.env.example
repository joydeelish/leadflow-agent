# ── Arc / Circle ────────────────────────────────────────────────────────────
# Orchestrator private key (buyer — spends USDC to pay agents)
ARC_PRIVATE_KEY=0x_your_orchestrator_private_key

# Specialist agent wallet ADDRESSES (sellers — receive USDC payments)
ENRICHMENT_AGENT_WALLET=0x_enrichment_agent_public_address
SCORING_AGENT_WALLET=0x_scoring_agent_public_address

# ── Agent URLs ──────────────────────────────────────────────────────────────
# Local dev
ENRICHMENT_AGENT_URL=http://localhost:3001/enrich
SCORING_AGENT_URL=http://localhost:3002/score
# Production (replace with Railway/Render URLs after deployment)
# ENRICHMENT_AGENT_URL=https://leadflow-enrichment.railway.app/enrich
# SCORING_AGENT_URL=https://leadflow-scoring.railway.app/score

# ── Agent Ports ─────────────────────────────────────────────────────────────
ENRICHMENT_PORT=3001
SCORING_PORT=3002

# ── OpenAI ──────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── WhatsApp / Meta Graph API ───────────────────────────────────────────────
WHATSAPP_TOKEN=your_meta_graph_api_token
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
# Business owner's WhatsApp number to receive qualified leads
BUSINESS_WHATSAPP_NUMBER=+2348012345678

# ── App ─────────────────────────────────────────────────────────────────────
PORT=3000
QUALIFY_THRESHOLD=70
