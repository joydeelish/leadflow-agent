# Arc / Circle
ARC_PRIVATE_KEY=0x_your_orchestrator_eoa_private_key
ARC_RPC_URL=https://rpc.arc.io

# Agent wallet ADDRESSES (public addresses — agents receive payments here)
ENRICHMENT_AGENT_WALLET=0x_enrichment_agent_evm_address
SCORING_AGENT_WALLET=0x_scoring_agent_evm_address

# Agent URLs (for production, replace with deployed URLs)
ENRICHMENT_AGENT_URL=http://localhost:3001/enrich
SCORING_AGENT_URL=http://localhost:3002/score

# Agent ports
ENRICHMENT_PORT=3001
SCORING_PORT=3002

# OpenAI (used by scoring agent)
OPENAI_API_KEY=sk-...

# WhatsApp / Meta Graph API
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=

# App
PORT=3000
QUALIFY_THRESHOLD=70
